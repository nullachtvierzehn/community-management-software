{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "current migrations",
      "outputCapture": "std",
      "program": "${workspaceFolder:Root}/node_modules/graphile-migrate/dist/cli.js",
      "cwd": "${workspaceFolder:Database}",
      "args": ["watch"],
      "envFile": "${workspaceFolder:Root}/.env"
    }
  ]
}
