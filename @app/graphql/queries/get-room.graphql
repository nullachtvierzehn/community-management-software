query GetRoom($id: UUID!) {
  room(id: $id) {
    id
    title
    abstract
    nSubscriptions
    latestItem {
      id
      contributedAt
      contributor {
        id
        ...ShortProfile
      }
      nthItemSinceLastVisit
    }
  }
}
