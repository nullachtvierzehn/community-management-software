query GetSpace($id: UUID!) {
  space(id: $id) {
    id
    name
    isPublic
    items(orderBy: [CREATED_AT_ASC]) {
      pageInfo {
        hasNextPage
      }
      nodes {
        times {
          currentApprovalSince
        }
        id
        createdAt
        editor {
          id
          ...ShortProfile
        }
        messageRevision {
          id
          editor {
            id
            ...ShortProfile
          }
          body
        }
      }
    }
    subscriptions {
      totalCount
      nodes {
        id
        createdAt
        subscriber {
          id
          ...ShortProfile
        }
      }
    }
  }
}
