mutation CreateSpaceItem($payload: SpaceItemInput!) {
  createSpaceItem(input: { spaceItem: $payload }) {
    space {
      id
    }
    spaceItem {
      id
      createdAt
      fileId
      messageId
      revisionId
    }
  }
}
